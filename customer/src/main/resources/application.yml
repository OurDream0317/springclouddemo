spring:
  application:
    name: Customer #注册中心中的名字
eureka:
    client:
      service-url:
        defaultZone: http://localhost:8761/eureka/  #当前项目要注册到那个注册中心
      register-with-eureka: false  #是否把自己注册到注册中心
      fetch-registry: true  #是否从注册中心获取信息
server:
  port: 8081

